# Spring Boot REST CRUD Employee Management System

This is a RESTful web service built using Spring Boot that provides CRUD (Create, Read, Update, Delete) operations for managing Employee data. The application demonstrates clean layered architecture using Controller, Service, DAO, and Entity layers with Spring
